import { Routes } from '@angular/router';
import { ExampelComponent } from '../components/example/exampel.component';

export const routes: Routes = [
  {
    path: '',
    component: ExampelComponent,
  },
];
